export default function MetaCard({
    client,
    year,
    description,
    tags,
  }: {
    client: string;
    year: string;
    description: string;
    tags: string[];
  }) {
    return (
      <div className="">

     </div>
    );
  }
  